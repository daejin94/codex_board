---
trigger: always_on
---

## 필수 선행 단계 (Context-Aware Reading)
1) **공통 문서 확인**: `docs/spec.md`, `docs/plan.md`, `docs/progress.md`, `docs/next.md`를 읽고 전체 요구사항과 우선순위를 파악한다.
2) **작업 성격별 상세 문서 확인**:
   - **기능 구현 시**: `docs/feature/` 폴더 내 관련 문서를 읽고 상세 요구사항을 파악한다.
   - **리팩토링 시**: `docs/refactoring/` 폴더 내 관련 문서를 읽고 개선 목적과 범위를 파악한다.
3) **작성 전 보고**: 코딩 시작 전, 파악한 현재 상태와 다음 수행할 작업을 요약하여 보고한다. 이때 관련 `.md` 파일들을 현행화(Update)한 후, 사용자에게 **'코드 작성 진행할까요?'**라고 질문한다.
4) **승인 기반 실행**: 사용자가 '네' 또는 긍정적인 답변을 했을 때만 실제 파일 수정 및 코드 작성을 시작한다.
5) **언어 설정**: 모든 대화와 문서 작성(주석 제외)은 **한글**로 진행한다.

## 프로젝트 명령어 (Windows PowerShell 기준)
- 설치: `npm install`
- 실행: `npm run dev`
- 테스트: `npm test` (존재할 경우)
- *주의: 모든 터미널 명령어는 PowerShell 문법을 준수하며, 경로는 슬래시(/) 또는 역슬래시(\)를 환경에 맞게 정확히 사용한다.*

## 규칙 및 컨벤션
- **언어 및 구조**: TypeScript를 사용하며, 비즈니스/도메인 로직은 반드시 `src/domain`에 위치시킨다.
- **출력 최적화 (비용 절감)**: 
  - 단순 폴더 생성, 파일 이동, 반복적인 보일러플레이트 코드는 '생각 과정' 없이 결과와 실행 명령어만 즉시 제공한다.
  - 알고리즘, 복잡한 상태 관리 등 설계 판단이 필요한 경우에만 핵심 사고 과정을 3줄 이내로 요약하여 보여준다.
- **명령어 제공**: 모든 커맨드는 터미널에서 즉시 복사/붙여넣기 가능하도록 독립된 코드 블록으로 제공한다.
- **백엔드 코드 설정**: 모든 백엔드 .java 코드 생성 시 UTF-8 BOM 포함하지 않고 생성한다.
- **에러 처리 관련**: 이슈를 제기한 에러를 원인과 이유 처리 결과를 정리하여 `docs/troubleshooting.md` 에 저장한다.

## 문서화 및 동기화 (실시간 원칙)

- **작업 중 (수시 업데이트)**:
  - `docs/plan.md`: 구현 과정에서 설계가 바뀌거나 단계가 추가되면 **즉시** 반영한다.
  - **상세 문서 현행화**: `docs/feature/` 또는 `docs/refactoring/` 내의 해당 작업 문서도 코드 변경 사항에 맞춰 실시간으로 업데이트하여 코드와 문서의 싱크를 유지한다.
  
- **작업 완료 및 세션 종료 시**:
  - `docs/progress.md`: 완료된 항목을 'Done'으로 이동하고 전체 진척도를 갱신한다.
  - `docs/next.md`: 현재 문맥을 바탕으로 다음에 바로 이어서 해야 할 구체적인 할 일을 적는다.
  - `docs/decisions.md`: 기술적 선택의 이유와 논리적 근거(Why)를 기록한다.